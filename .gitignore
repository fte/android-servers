# Stock firmware: 859 M, over git's file limit — the repo keeps firmware/g386f-ol1.zip
# (that file is the only thing Odin needs; images are re-extracted with `unzip` + `simg2img`,
# README §8.1). Extracted images are deliberately NOT versioned.
firmware/g386f-ol1.zip
firmware/*.img
firmware/*.tar.md5

# Local `adb pull /system syspull` tree used by scripts/walk-deps.py
syspull/

# Regenerated by `sh scripts/install-acc.sh download`
payload/sums

# Python / macOS
__pycache__/
*.pyc
.DS_Store
